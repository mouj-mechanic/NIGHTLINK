{
  "mode": "namespace",
  "namespace": "cursor-subscriptions",
  "namespaceStatus": "ready",
  "tools": [
    {
      "tool": "list_subscriptions",
      "description": "List active event subscriptions for this Cloud Agent.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "tool": "subscribe_linear_comment",
      "description": "Subscribe this Cloud Agent to comments created on Linear issues, scoped to one team or to one individual issue. Delivers one notification per new comment, with that comment's text. It does NOT deliver comment edits, resolutions, or reactions, and it does NOT deliver comments on Linear documents, project updates, or initiative updates (those carry no issue and are dropped). A delivery is one comment, not a thread: earlier replies in the same thread are not included, so read the issue if you need the conversation so far. Project scope is not offered because Linear's comment webhook does not say which project the issue belongs to. Scope ids accept the resource's linear.app URL, its UUID, or its copyable ref — an issue identifier like CHAR-661, or a team key like CHAR — and must belong to the Linear organization your account is connected to. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "options": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "team"
                  },
                  "teamId": {
                    "type": "string",
                    "description": "The team's linear.app URL, UUID, or key (e.g. CHAR)."
                  }
                },
                "required": [
                  "scope",
                  "teamId"
                ]
              },
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "issue"
                  },
                  "issueId": {
                    "type": "string",
                    "description": "The issue's linear.app URL, UUID, or identifier (e.g. CHAR-661)."
                  }
                },
                "required": [
                  "scope",
                  "issueId"
                ]
              }
            ]
          },
          "expiresInSeconds": {
            "type": "number"
          }
        },
        "required": [
          "options"
        ]
      }
    },
    {
      "tool": "subscribe_linear_issue",
      "description": "Subscribe this Cloud Agent to Linear issue events on one team, one project, or one individual issue. Delivers exactly two kinds of event: an issue being created, and an issue's workflow state changing. It does NOT deliver any other edit — title, description, labels, assignee, priority, estimate, cycle, comments, and backlog reordering all produce no notification (Linear fires an update webhook for every one of those, so delivering them would be noise). Comments have their own subscription tool. Each delivery reports one issue's one change; it is not a digest and says nothing about the team's other issues. Project scope only matches issues that already carry that project, so an issue created without a project and filed into one later is not delivered to a project subscription. Scope ids accept the resource's linear.app URL, its UUID, or its copyable ref — an issue identifier like CHAR-661, a team key like CHAR, or a project slug — and must belong to the Linear organization your account is connected to. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "options": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "team"
                  },
                  "teamId": {
                    "type": "string",
                    "description": "The team's linear.app URL, UUID, or key (e.g. CHAR)."
                  }
                },
                "required": [
                  "scope",
                  "teamId"
                ]
              },
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "project"
                  },
                  "projectId": {
                    "type": "string",
                    "description": "The project's linear.app URL, UUID, or slug (e.g. improve-onboarding-a79f0fd1671d)."
                  }
                },
                "required": [
                  "scope",
                  "projectId"
                ]
              },
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "issue"
                  },
                  "issueId": {
                    "type": "string",
                    "description": "The issue's linear.app URL, UUID, or identifier (e.g. CHAR-661)."
                  }
                },
                "required": [
                  "scope",
                  "issueId"
                ]
              }
            ]
          },
          "expiresInSeconds": {
            "type": "number"
          }
        },
        "required": [
          "options"
        ]
      }
    },
    {
      "tool": "subscribe_origin_ci",
      "description": "Subscribe this Cloud Agent to terminal Origin CI results on a repository, or on one branch of it. Waits until every check run on a commit is terminal, then delivers one commit-wide result: success when none failed, or failure with the failed check names. Only commits that are an open pull request's head are covered, so the branch scope matches PR head branches; CI on commits with no open PR does not produce events. Failures always deliver; a success delivers only as this subscription's first result or when it flips a previously delivered failure — repeated greens are not delivered. Subscribing to the repository's default branch (e.g. main) is one-shot: it delivers the next terminal result on that branch and then closes itself. To wait for a PR's CI, subscribe to the PR's head branch instead. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "options": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "repo"
                  },
                  "repo": {
                    "type": "string"
                  }
                },
                "required": [
                  "scope",
                  "repo"
                ]
              },
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "branch"
                  },
                  "repo": {
                    "type": "string"
                  },
                  "branch": {
                    "type": "string"
                  }
                },
                "required": [
                  "scope",
                  "repo",
                  "branch"
                ]
              }
            ]
          },
          "expiresInSeconds": {
            "type": "number"
          }
        },
        "required": [
          "options"
        ]
      }
    },
    {
      "tool": "subscribe_origin_pr",
      "description": "Subscribe this Cloud Agent to future Origin pull request events. Not delivered: PR lifecycle events that echo this agent's OWN actions — creator-actored events on this agent's own working branch (its own pushes/synchronize, closes; Origin agents act as their owner) — and metadata-only edits (title/body/base changes). Always delivered: all comments and reviews — including Bugbot and Agentic Security Review activity, which posts from app identities — plus lifecycle events actored by third parties or on any other branch. A PR-scoped subscription (scope: pr) closes itself once the PR is merged or closed. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "options": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "pr"
                  },
                  "prUrl": {
                    "type": "string",
                    "description": "An Origin pull request URL (https://cursor.com/codebase/<owner>/<repo>/pull/<n>)."
                  }
                },
                "required": [
                  "scope",
                  "prUrl"
                ]
              },
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "pr"
                  },
                  "repo": {
                    "type": "string"
                  },
                  "pullNumber": {
                    "type": "number"
                  }
                },
                "required": [
                  "scope",
                  "repo",
                  "pullNumber"
                ]
              },
              {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "repo"
                  },
                  "repo": {
                    "type": "string"
                  }
                },
                "required": [
                  "scope",
                  "repo"
                ]
              }
            ]
          },
          "expiresInSeconds": {
            "type": "number"
          },
          "keepWatchingAfterClose": {
            "type": "boolean",
            "description": "Only for scope: pr. By default a PR subscription closes itself when the PR is merged or closed. Set true only when the user explicitly asked to keep watching this PR after it is merged or closed (e.g. for post-merge review comments). Ignored on other scopes."
          }
        },
        "required": [
          "options"
        ]
      }
    },
    {
      "tool": "subscribe_slack_channel",
      "description": "Subscribe this Cloud Agent to future Slack channel messages. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "channelId": {
            "type": "string"
          },
          "topLevelOnly": {
            "type": "boolean",
            "description": "Ignore messages posted in threads."
          },
          "expiresInSeconds": {
            "type": "number"
          }
        },
        "required": [
          "channelId"
        ]
      }
    },
    {
      "tool": "subscribe_slack_new_channels",
      "description": "Subscribe this Cloud Agent to newly created public Slack channels in the linked workspace. Optional channelNameContains filters by a case-insensitive substring of the channel name. Re-subscribing updates the filter. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "channelNameContains": {
            "type": "string",
            "description": "Only match channels whose name contains this case-insensitive substring. Omit to match every new public channel."
          },
          "expiresInSeconds": {
            "type": "number"
          }
        }
      }
    },
    {
      "tool": "subscribe_slack_thread",
      "description": "Subscribe this Cloud Agent to future Slack thread messages. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "channelId": {
            "type": "string"
          },
          "threadTs": {
            "type": "string"
          },
          "expiresInSeconds": {
            "type": "number"
          }
        },
        "required": [
          "channelId",
          "threadTs"
        ]
      }
    },
    {
      "tool": "subscribe_timer",
      "description": "Subscribe a timer that enqueues the given prompt as a follow-up that runs when this Cloud Agent is otherwise free. Provide exactly one of `cron` (a cron expression) or `delaySeconds` (a fixed interval, in seconds). By default the timer recurs on each fire while active. Set `once: true` (with `delaySeconds`) for a one-off timer that fires a single time after the delay and then removes itself — e.g. \"remind me in 1 hour\". Dedupes by name. List and remove timers via list_subscriptions / unsubscribe. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "prompt": {
            "type": "string"
          },
          "cron": {
            "type": "string",
            "description": "Cron expression for recurrence. Provide this OR delaySeconds, not both."
          },
          "delaySeconds": {
            "type": "number",
            "description": "Interval, in seconds. For a recurring timer this is the fixed interval between fires; with `once: true` it is the one-off delay before the single fire. Provide this OR cron, not both."
          },
          "once": {
            "type": "boolean",
            "description": "When true, fire exactly once after `delaySeconds` and then remove the timer. Requires `delaySeconds` (not valid with `cron`). Defaults to false (recurring)."
          }
        },
        "required": [
          "name",
          "prompt"
        ]
      }
    },
    {
      "tool": "unsubscribe",
      "description": "Close one active event subscription for this Cloud Agent.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "subscriptionId": {
            "type": "string"
          }
        },
        "required": [
          "subscriptionId"
        ]
      }
    }
  ]
}